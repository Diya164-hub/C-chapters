#include <stdio.h>

int main(){
    // Ternary Operator in C
    // syntax : condition ? expression1 if-true : expression2 if-false;
    int a = 30;
    int b = 20; 
   a>b?printf("a is greater\n"):printf("b is greater\n");

    return 0;
}